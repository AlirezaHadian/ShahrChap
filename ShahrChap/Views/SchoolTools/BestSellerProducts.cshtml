@model IEnumerable<DataLayer.Factor_Details>

<div class="bestsellers">
    <div class="title">
        <h4>پرفروش ترین لوازم تحریر ها</h4>
    </div>
    <div class="underline-bestsellers"></div>
    <div class="owl-carousel owl-theme">

        @foreach (var item in Model)
        {
            @Html.Partial("_Product", item.Products)
        }

    </div>
</div>